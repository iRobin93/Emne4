<Window x:Class="MediaPlayerApp.EditPlaylist"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MediaPlayerApp.Data"
        mc:Ignorable="d"
        Title="EditPlaylist" Height="450" Width="800">
    <Grid>
        <!-- Bind to Playlists.playlists (List of Playlist objects) -->
        <ListBox ItemsSource="{Binding Source={x:Static local:Playlists.playlists}}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <!-- Display Playlist Name -->
                        <TextBlock Text="{Binding Name}" FontWeight="Bold" />

                        <!-- Bind to Songs within each Playlist -->
                        <ListBox ItemsSource="{Binding Songs}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Title}" />
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
