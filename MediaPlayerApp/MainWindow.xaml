<Window x:Class="MediaPlayerApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MediaPlayerApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />  <!-- Header -->
            <RowDefinition Height="*" />  <!-- SubPage Container -->
            <RowDefinition Height="Auto" />  <!-- Footer -->
        </Grid.RowDefinitions>
        <Border Grid.Row="0"
        Background = "LightBlue"
        Padding="10">
            <StackPanel VerticalAlignment="Center" >
                <TextBlock>This is a header!</TextBlock>
                <Button Content="Administrer" HorizontalAlignment="Right" x:Name="AdminButton" Click="AdminButton_Click" />
            </StackPanel>
        </Border>
        
        <!-- Frame where pages will be loaded -->

        <Frame Name="MainFrame" JournalOwnership="OwnsJournal" Grid.Row="1" NavigationUIVisibility="Hidden"/>

        <Border Grid.Row="2"
        Background = "LightBlue"
        Padding="10">
            <TextBlock FontSize="20">This is a footer!</TextBlock>
        </Border>
    </Grid>
</Window>
