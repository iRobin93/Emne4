<Window x:Class="MediaPlayerApp.AdministratePlaylists"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MediaPlayerApp.Data"
        mc:Ignorable="d"
        Title="AdministratePlaylists" Height="450" Width="800">
    <Grid>
        <!-- Bind to Playlists.playlists (List of Playlist objects) -->
        <ListBox ItemsSource="{Binding Source={x:Static local:Playlists.playlists}}" Margin="0,0,205,148">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <!-- Display Playlist Name -->
                        <TextBlock Text="{Binding Name}" FontWeight="Bold" Width="66" />
                        <Button Content="Endre" Click="Button_ClickEdit"/>
                        <Button Content="Slett" Click="Button_ClickDelete"/>
                    </WrapPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
